.contents.row
  %p
    = @name
    の投稿一覧
  - @posts.each do |post|
    .content_post{:style => "background-image: url(#{tweet.image});"}
      = simple_format(post.text)
      %span.name= post.name
  = paginate(@posts)
